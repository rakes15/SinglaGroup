<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="@dimen/padding64dp"
    android:layout_height="@dimen/padding64dp"
    android:layout_margin="@dimen/padding2dp"
    android:foreground="?attr/selectableItemBackground"
    android:theme="@style/AppTheme"
    card_view:cardCornerRadius="@dimen/padding5dp"
    card_view:contentPadding="@dimen/padding1dp">
    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
        <TextView
            android:id="@+id/textView_Count"
            style="@style/CustomSmallTextViewHeader"
            android:layout_centerInParent="true"
            android:layout_gravity="center_horizontal|center_vertical"
            android:gravity="center_horizontal|center_vertical"
            android:textColor="@color/Black"
            android:text="100"
            android:textStyle="bold"
            android:padding="@dimen/padding5dp"/>
        <TextView
            android:id="@+id/textView_header"
            style="@style/CustomTinySmallTextViewHeader"
            android:layout_width="match_parent"
            android:textSize="@dimen/txtSize"
            android:textColor="@color/Black"
            android:layout_alignParentTop="true"
            android:gravity="center_horizontal|center_vertical"
            android:text="100"/>
    </RelativeLayout>
</android.support.v7.widget.CardView>